{
I want to add a layer feature, where each data display in the viewer will be selectable and the use can toggle visible or hide. 

I want to add a button in the tool bar to add custom data trought the browser. This option should trigger a menu to either add a link to some data or to upload the data directly in the browser. 
}

{
ok this is nice, it does exacly what I need, Now I dont like the rename button. I dont like that it's not a drag and drop. So two things :

I'd like to have a 6 dots to the left where the user drag and drop like other drag and drop that exist on the web. I would like to have a smooth animation and I would like to have a smooth list where the element move when I hover in a place to let the new element drop in. I think this type of drag and drop exist online, if not we could program it. 
Secondly, I'd like to have a 3 vertical dot at the end of the line for each element. This button on click should onpen a contextual menu where I can edit the name (rename), or delete. For now these will be the only two options.    
}

{
In LayerPanel.tsx, I changed this:
import {
  closestCenter,
  DndContext,
  DragEndEvent,
  DragOverlay,
  MouseSensor,
  TouchSensor,
  useSensor,
  useSensors,
} from "@dnd-kit/core";

to this:
import {
  closestCenter,
  DndContext,
  DragOverlay,
  MouseSensor,
  TouchSensor,
  useSensor,
  useSensors,
} from "@dnd-kit/core";
import type { DragEndEvent } from "@dnd-kit/core";

And I solve an error I had. 

Now I have a few comments/correction on the current code, 
(1) The drag and drop doesnt really works. when I try to drag the basic layer into a a new folder I juste created it refuse to do it. I think it's because the drag and drop try to put it as root. Ok so I juste tested and whe the basic slice plane is at root, now I can drag and drop and it works fine. So I guess I need a new feature where when I hove over a group, it should highlight the group and if I drop the layer, it should put the layer into this group. Or the user can also drag to the end of the list to put the layer in the root (nothing special here, the user is just moving hte layer to the bottom of the list and the layer is not on top of a group)  

(2) When I click the 3 vertical points, the contextual menu appear, but when I onfocus this menu it should disappear automatically. Now it only dissapear if I click on an option or if I click on the 3 dots again. So now if I click on the 3 dots of every layer there's multiple contextual menu and it's not greate. Only one open at a time and one menu should close when click away. 

(3) I dont need the "move to..." if the (1) is resolve where I can already move the layer to a group. 

(4) I the user is trying to move a group it should move all the element under the group with it. this should also work if the user is movin an group into another group. 

(5) Now the hide button does not work at all. The hide button should work for a single element and should also work for a group. So every element inside the group should be at the same state as the root of this group. The icon hide and visible should be synch (even if the user hide the group, the icon of the element inside should all switch to hidden). Finally the element should be highlighted when selected in the list of layer. the highlight should be apply to the element under the hirarchy.

Can you correct all these points?


}

{
ok one last thing the section of the layer is overflow-y:auto. I see the scrolling bar sometimes. I would prefer just visible. I dont care if it fell a bit ouside the componenent
}


{
ok one more thing, for now when I have multiple layer inside. I want to be able to close the group. That way I wont always see every group and every layer. If the user hover over a group and it's close, it should open the groupe so the user can hover and open the group. A bit like the file architecture works in vscode. 
}

{
I have a bug right now. When I have a layer inside a group adn the groupe is hidden, the user cannot make visible the element inside this group. It's a bit non intuitive to have to open the group visibility to see the element. Each element should have a state. and if the use ask to see the layer, it should show the layer by switching the elemtent to visibile. 

Also now when I make on element visible, it make every layer underneeth visible too. But if the user have hide some elment inside the group, and make visible the top group, the groupe inside should say in the same state (hidden ins this case) so the user have a better controle and the setting should stay the same.   
}


{
I also want to be able to close completely the layers section. For someone annotating it can be annoying to always see the layer section display. So I would like to have a nice animation to open and close the layer. When close it should be only one button with the layer icon.

Also the layer section should have a max height and I would like to have a nice scroll bar that fist with the rest of the ui. Right now it's the basic white and gray full squared scroll bar and its ugly.  
}

[todo] add allen data (volumetric and layer at high resolution)
{
ok greate thanks for the drawing comment I  will do it later. For now we are working on importing data. So there are some small tweak I want for the ui. 

(1) For now the groupe have an arrow to open an close the group. The layer dont but there's is still a space like if it was a group, but there's now chevron to open and close. This is a bit confuising. The layers should not have this space. The 6 dots to drag and drop and the eye to toggle visible hidden should be side by side. 

(2) For the add layer prompt, I would like to have a better visual. One thing we can do is to add icons to better guide the user. Can you add in front of the "Drwaing layer" a pencil, in front of the "Exterenal source" a link, and in front of the "custom data" a data icon.  

(3) I dont really like the external source panel. Instead of having field the user has to complete, I would rather have a grid of clickable button. On the button I want to have icons and I want to have a the title of what the source is. For now the icon can be empty or a generic one, but I will create some icons for each time of source later. One a user click on one element in the grid it should select the element and the the user can click on add layer. The user can select multiple element at one to add layer, this should create multiple layers. In addition the user can link it's own external source, to do that there should be a button somewhere on this panel where the user can add a new extrenal source. Now the user will be ask to enter the url and the name of the source and the source should be add into the list of element in the grid. The icon for this new element should be a "custom" icon  

Can you update these element, then we can actually do the data import into the viewer.
}

{
alright not perfect, but a good start. Now the first we need to fix is the slices view. At the moment, when I select the allen average mouse brain (slices) I see the three main axis image on the bottom right and I also see the three main axis in the 3d view. I want to have the images in the 3d view. The goal here is to display the data in 3d and the user could change the slice as he want to see the data. So the user can change the cut into  the brain to see the data of the average mouse brain at a specific slice. For now I only want to have the 3 main slices with the 3 main axis. We can add the custom slice and how the user move the slice in dpeth later.    
}

{
    alright it works, but there one axis that is in the wrong orientation. The axis back of the brain to front of the brain is in the wron orientation. Could it be because in the zarr format, there's one axis that is suppose ot be flipped ? Do you think the probleme is in the dat or in the vewer? Can you fix the orientation on this axis? 
}

{
    hum ok the render does flip the image, but the image is display on a slice (3d rectangle) but this rectangle does not change at all and since it's a rectangle not all side are the same dimension, the image get deformed. So it's not much a problem of the renderer is more a problem of the data not being displayed correctly. I feel like the current solution is a bit invasive where we need to add custom transformation directly into the webgl viewer instead of having tranformation for the data. Overall web gl was doing it's job of displaying the data, it's more a probleme with how we want to display the data and it seems the allen mouse brain has one axis that does not match with the other which should be flip and 90 rotated. 

    Can you propose a better solution ?  and maybe reverting some of the change in the webgl renderer ?
}
{
  alright now I have a strange thing that happen. the axe we just corrected ia bit to small compare to the other axis. The volume is 3d rectangle and right now one axis is smaller and it does not create a rectangle box. Can you check in the code what is happening and correct the probleme ? 
}
[HERE]

[todo] add a custom cut the user want to load in the viewer.
{
alright not bad for a start. Now there's a couple thing wrong with this : 

(1) I need to add custom slice at differnete orientation. So right now we only allow these 3 axis xy,xz,yz. I need more. I want the user to define any slice at any angle of orientation. So here is my situation. I want to be able to define any slice in the 3D space. Can you recommend me a way for the user to define this 3d plane intuatively and easly. I wush the user could only use it's mouse and key board to do that. 

(2) the panel for "custom slice" is nice, but right now it's in the way and it's always on. Instead of that, I have a tool bar where I define some tools. The slice will be added as a tool. So I want you to move this panel to the tool bar with the other tool. When click on the icon (we should find an icon suitable to represent the slicing tool) ther panel to contruct the slice should appear on top with a nice smooth transition. Then the user can add the parameter for the slice a click on create. This menu should be close if unfocus. 
}

{
ok that's nice now the tool bar has the tool. For starter now the slices added image are not synch with the allen aberage mouse brain (slice) volume. I need both source of data to be in the same orientation and now when I add a slice I see that the orientation of the brain is flipped. Can you fix that so the image are in the same position as the average mouse brain (slice)? 

Make sure the slice generator generate slice with the same data pattern as the allen average mouse brain (slice). The slice generator should be synch and the pixel of a slice should be an extraction of the whole volume. At the moment the front and back of the mouse are inverted. Do not change the webgl viewer. This change should be done related to the data and not the webgl viewer (I mean it's possible this fix has to touche to the webgl, but right now it's not webgl the problem)

Also. I dont like the icon. I would rather have a cut icon.  

can you fix these first and then we can continue with the any planes feature? 
}

{
    alright so it did add a custom angle which is nice, now we have a probleme with the image flip or rotation still with this new feature. We had this kind of prebleme before and we solve it using a custom load sequece to make sure every pixel is in the right place. We user this setting to load the allen data  : 

    export const ALLEN_VIEWER_PROFILE: ViewerOrientationProfile = {
  xy: {
    reverseIndex: true,
    flipX: true,
  },
  xz: {
    flipY: true,
  },
  yz: {
    rotate90: true,
    reverseIndex: true,
  },
};

I think we need to use this exact setting so the image extracted fits because right now its upside down.  

I also have a scaling problem. I check on the Z axis and extracted a slice at 0 and compare this slice to the allen template I have and the extracted image was smaller. Can you also make sure the scaling is good too ? 

Here are my current code files : 
// src/omeZarr.ts
// src/WebGLCanvas.tsx

}
{
    Ok now it works... almost ! The scale is ok . Now the only thing that is missing is now the front and the back of the brain are flipped. This only happens when I select custom orientation. When I extract with the three main axis the orientation is good, but with the custom orientation i messes the orientation where the back on the brain is in the front comapre to the allen averame mouse brain slice. Can you check where this miss match can come front and adjsut the code ?  
}
{
    You will be mad, becaus nothing has changed for my problem. So I did a little more testing and I realize the it's only when I extract a slice in the y axis that I have a flip. When I extract data from any other axis everything run smoothly, But as soon as the y axis is not 0 I got a flip. Can you check more spcifically for a bug with the y axis of the costum orientation? 
}
[todo] save and update the state of browser (viewer) (export). 
[todo] the user should be able to change the current cuts how he likes.  

[todo] volumetric
[todo] upload data
[todo] shared button 

