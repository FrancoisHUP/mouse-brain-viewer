
[todo] import a skeleton of the allen average mouse brain and the annotations (slices, volumetric and shell). 
{
    perfect I corrected small bugs, not I need to update my frontend code. RIght now the current state of my project dosent handle skeleton mesh. I can only load volumetric and allen average mouse brain (with the image on axis). So I need to add two things :

    (1) I need to add the skeleton of the brain. For that I think I need to update the webgl component to render the skeleton on the scene and other files. 

    (2) I need to add the annotations of the allen average mouse brain. FOR NOW DO NOT IMPLEMENT THIS ONE. Since this import have more requirements, we can ignore this one for now and come back at it later. 

    Both of these has to be imported in the importpanel where I have the source the user whats to uplaod.  

    Here are my current files. For now lets just focus on the skeleton render and import. 
}
{
    ok so the "ALLEN_MESH_POSITION_OFFSET" is a bit wierd, I play with the numbers and I got to this point : 
    const ALLEN_MESH_POSITION_OFFSET: [number, number, number] = [0, -7700, 0];
    Where now the mesh is approximatly in hte right place. I feel like the -7700 is a bit wierd like a settings. 
    But at this point the orientation is all fine, but theres one other thing that dont work : the scale is not right in the front and back axis of the brain. I need to scal only one axis. Can you fixed both of these. Also you are allow to refactor the code to streamline the setting and or refactor to have more clean code 
}
{
    Now if you remeber, I was asking for the mesh and the annotations. Now let's move on to the annotation part. I have the ome zarr annotation file at this google cloud bucket path "https://storage.googleapis.com/sbh-assistant-data/allen_annotation.ome.zarr/". I need to add a new import to the import panel and I need to import the allen annotation. Now the transformation is always a pain in the ass. I would like to no have to do this. Opefully we already have the transformation I apply to the allen data in here "src/omeZarr.ts". So here is the requirements for the annotations :

    (2) I need to add the annotations of the allen average mouse brain. For this I want to be able to overlay the annotation on top of the allen averag emouse data. I know that if you place two 3d object at the same space, the webgl gub and we see artefact of both image, but it's not visually appealing, since we dont see clearly both. So here I need a way to view the annotations + the allen average mouse brain. I want the user to be able to chose the resolution of the import. Right now I have desable the 10um, beacause the file is to heavy for the browser. Let's do the same for the annotation. So even if the buckets have the 10um resolution, we dont show this resolution to the user, because the browser is not able to handle that amount of data. 

    ok so I have on more requirement concerning the allen annotation. You tell if it's a good idea and how hard it is to implement : THe allen annotation are multiple region and sub regions of the brain. I would like the user to be able to load all the the annotation at the same time, But I would also like the user to be able to controle the annotation visibility. I already have a system of layers where I can toggle a layer visibility. This layer have a three like structure. My plan would be to add the annotation into a "folder" (group of annotation) all at the same time and when the user click on this folder et would see all the regions in the three like structure like the annotation of the allen are organised. Then the user could toggle visibility of a sub region or even controle the regions visibility.
}
[todo] making the annotation match the color of the allen atlas; getting the other skeleton of the annotation instead of volumetric data or both ? 
[todo] add all regions (annotation) at the same time in a three like structure into the layers panel 

{
    ok one more small thing. Now when I select an element in the list of "Select external sources" the element that have a resolution now change the ui when selected. The button become rounded white baground. I think this has change when we were trying to fix the selected resolution. The problem I have is noe that the button change ui, it changes the space the text take and now the text skip a line. This cause the element to change height and it change the layout of the element. So it looks like it's glitching. Can you check if you can do something so this glitch doesnt happen?   
}
[todo] contextual menu for navigation (toogle between the camera moovement)
{

}

[todo] The layers should have some setting (opacity, any transformations) and infos (name slice, resolution, dimesnion). On selected layer, the ui should display some setting of this layer. This will also serve as a first step to the point, click and move object in 3D.  

[todo] add a temporary visualize of the cut slice : this visual should be a normal poiting in the direction with the depth. The image should be display and with the new parameter, it should adjust the image slice. Instead of number, it should be a slider or something easyer to play with. (rework the icon too, mabe a 3D cube with a cut? create it with bananas)

[todo] draw (annotations) with AI 3D assistance (where the annotations is save?, in the browser)
[todo] upload data
[todo] shared button 
[todo] in the profil, the user can open saved states

-----

[NTH] change the camera controle (flying, or the others)
[NTH] add a docs route where the user can see all the controls on this web demo. 


[LATER] Object control. The user should be able to change the current cuts how he likes.  The user should be able to change the orientations and position of objects in the scene. He should also be able to group things togheter. 
{
    That is absolutely perfect. Now I want to start a new feature ! 
    
    I want the user to be able to apply rotation and translation to an object of the scene. I also want the user to be able to group object togeter. I want the grouping to be apply in the layer too. So if a user click to select multiple object and group them (which should be an option with the right click of the mouse) the groupe should aslo happen in the layers panel. This will help the user to align thing togheter and move things around.

    Alright here is the more specific requirements :
    (1) I need to add a tool in the bottom tool bar. This one will be selection with an appropriate icon.  
    (2) When the selection sool is selected in the toolbar, the controle should switch to other then typical the moving controle. I need to have these controls : left click to select the object in the position of the mouse; left drag and drop to select multiple object at the same time (this should display a square between the mouse click and hold to the current mouse position) - while the user hold and move the mouse, it should hightlight the selected object - every object that fall into the square should be slected - on release of the mouse, it should have selected all the object in the square; the right mouse button should not be the typcial chrome menu - the right mouse click should open a contextual menu with these option : ["delete", "group"]. and object can be a group of objects. When the user select one single object (which can be a group of sub-object, but we count it as one obejct since they are grouped), then the ui will highlight this object and arrows will appear. In a case where the user select muliple non group object, the arrow still appears and the object behave as they where a group temprarly. In total there's 6 arrows to controle the translation in each main direction (x,y,z), plus I could like to have 6 more arrows to controle the orientation and finally I need 6 arrows to control the rotation on one axis. These arrow should be place like this : for all object selected, we should create an ivisible cube that contains all the objects selected - all the obejct should be inside this invisible cube - then we take 3 side of this cube (the 3 side closer to the camera) and at the center of the face of the invisible cube we add the arrow to control the position. if that make sense. Basiaclly the same thing as every CAD sofware that exists. I would like to have the objects snap to the orthogonal angles, so its easier for the user to get perfect angles. 
}

--- small fixes
[fix]

--- BUG LIST
[bug]{
Alright now I have a bug relating the volumetric data. The problem is when a slice layer in order under the layer of the volumetric data, the slice does not appear. Its either the slice is there, but the rain volume is on top, or something worst...  
}

[bug]{
    the volumetric data flip upside down when the camera is tilte enough up and down. 
    {
        ok bug resolve, now I have a really wierd bug visually in the webgl scene. Now I see the annotations, but with a certain angle I see the annotation flip upside down. This happend when the camera is pointing to high or to low compare to the volume of the brain. So if I place the camera right in fornt of the annotation I see the annotation in the right angles, and when I stilt the camera up, at a certain angle it change the annotation upside down. THe smae happend if the camera is pointing to low. This is only happening with the annotation.
    }
}




